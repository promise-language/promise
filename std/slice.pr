// std/slice.pr — slice[T] type methods
type slice[T] `native {
    int len;

    [](int index) T `native;
    []=(int index, T value) `native;
    [:](int? start, int? end) T[] `native;
    [:]=(int? start, int? end, T[] value) `native;

    push(T elem) `native;
    pop() T? `native;
    contains(T elem) bool `native;
    remove(int index) `native;

    get is_empty bool => this.len == 0;
}
