type Box {
  int n;
  derive(this, string v) string {
    return v + "!";
  }
}
mk(Box b, string v) Task[string] {
  return go b.derive(v);
}
main() `test(expected: "ab!") {
  b := Box(n: 1);
  task[string] x = mk(b, "a" + "b");
  string r = <-x;
  print(r);
}
