// std/ordered.pr — Structural ordering interface (extends Equal)
type Ordered is Equal `structural {
    <(Self other) bool `abstract;
    >(Self other) bool `abstract;
    <=(Self other) bool `abstract;
    >=(Self other) bool `abstract;
}
