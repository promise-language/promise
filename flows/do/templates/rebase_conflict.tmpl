{{.ItemHeader}}

Your committed change for this item was rebased onto the latest origin, and the
rebase STOPPED ON MERGE CONFLICTS. Resolve them so the rebase completes on top of the
remote.

Conflicted files: {{.Conflicts}}

Rebase output:
{{.RebaseOutput}}

{{.RebaseResolution}}
- The re-verify must show 0 test failures and 0 leaks (the zero-leak policy is absolute;
  never add `allow_leaks: true`). If you changed Promise/stdlib modules, run `bin/build`
  first to re-embed them before re-verifying.

{{.AskGuidance}}
