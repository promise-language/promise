main() `test(expected: "300") {
  int a = 100;
  int b = 200;
  ch := channel[int](capacity: 1);
  go {
    ch.send(a + b);
    ch.close();
  };
  result := <-ch;
  if v := result {
    print_line(v);
  }
}
