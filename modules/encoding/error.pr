type EncodingError is error `public `doc("Error raised when decoding a binary-to-text encoding fails.") {
  int at_index `doc("Index into the input string where the failure was detected (-1 when the failure is not tied to a position, e.g. odd input length).");
}
