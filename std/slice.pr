// std/slice.pr — slice[T] type methods
type slice[T] `native {
    int len;

    push(T elem) `native;
    pop() T? `native;
    contains(T elem) bool `native;
    remove(int index) `native;

    is_empty() bool {
        return this.len == 0;
    }
}
