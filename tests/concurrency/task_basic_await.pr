// Basic task: spawn a function as goroutine, await result.
// Tests task[T] creation and value retrieval.

compute() int { return 42; }

main() `test(expected: "42") {
  t := go compute();
  result := <-t;
  print_int(result);
}
