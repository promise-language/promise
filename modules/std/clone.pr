// std/clone.pr — Structural cloneability interface
type Cloneable `structural(protocol: true) `public `doc("Types that can produce an owned deep copy of themselves via clone().") {
  clone() Self `abstract;
}
